export interface TableRow {
  won: number;
  lose: number;
  name: string;
  games: number;
  rank: number;
  diff: number;
  id: string;
  logoUrl: string;
  draw: number;
  goals: string;
  points: number;
}